<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kosanku.kelompok6.kosanku">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".TambahPenghuniActivity"
            android:theme="@style/AppThemeLogin"/>
        <activity android:name=".PenghuniActivity" />
        <activity android:name=".HapusActivity" />
        <activity android:name=".BangunanActivity" />
        <activity android:name=".KamarActivity" />
        <activity android:name=".TambahBangunanActivity" />
        <activity android:name=".PemasukanActivity" />
        <activity android:name=".PengeluaranActivity" />
        <activity android:name=".EditBangunanActivity"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>